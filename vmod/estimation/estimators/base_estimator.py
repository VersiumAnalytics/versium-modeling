"""base_estimator.py: Defines the base estimator class and base pipeline class. For now just use sklearn classes, but these can be overridden
in the future if necessary
"""
from sklearn.base import BaseEstimator
from sklearn.pipeline import Pipeline
